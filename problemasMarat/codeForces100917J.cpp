#include <bits/stdc++.h>
using namespace std;


int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int tam;
    int a[tam];
    int b[tam];
    int p,q;
    int dp[tam][p];
    int dp2[tam][q];
    bool first, second;
    cin>> tam;


    return 0;
}
